🚗 LibyaParts - المنصة الأولى لقطع غيار السيارات في ليبيا
الإصدار: 1.0.0
الحالة: جاهز للنشر التجريبي (Beta)
التطوير: تم تطويره بواسطة فريق التطوير المتقدم (Deepmind Agent)

📋 نظرة عامة
LibyaParts هي منصة ويب متكاملة تهدف إلى تسهيل عملية البحث عن قطع غيار السيارات في السوق الليبي. تربط المنصة بين تجار قطع الغيار و الزبائن الباحثين عن القطع، مما يوفر الوقت والجهد ويضمن الحصول على أفضل الأسعار.

تتميز المنصة بتصميم عصري (Premium Design) يدعم اللغة العربية بالكامل، مع التركيز على سهولة الاستخدام وسرعة الأداء.

✨ المميزات الرئيسية
🛍️ للزبائن
البحث المتقدم: إمكانية البحث عن القطع حسب الاسم، ماركة السيارة، الموديل، أو التصنيف.
التواصل المباشر: زر "طلب عبر واتساب" يفتح محادثة مباشرة مع التاجر مع رسالة جاهزة بتفاصيل القطعة.
إرسال الطلبات: إمكانية إرسال طلب لقطعة غير متوفرة، ليصل إشعار للتجار بتوفيرها.
تصفح سهل: واجهة مستخدم بديهية تعرض القطع المميزة والأحدث.
🏪 للتجار
لوحة تحكم خاصة: إدارة شاملة للمخزون (إضافة، تعديل، حذف القطع).
إدارة الطلبات: استلام طلبات الزبائن والرد عليها.
إحصائيات فورية: متابعة عدد القطع المعروضة والطلبات الجديدة.
نظام المصادقة: حماية لوحة التحكم بتسجيل دخول آمن.
👑 للمدير (Super Admin)
السيطرة الكاملة: لوحة قيادة مركزية لرؤية كل ما يحدث في المنصة.
إدارة المستخدمين: الموافقة على انضمام التجار الجدد أو رفضهم.
إحصائيات شاملة: تقارير عن عدد المستخدمين، التجار، والطلبات.
نظام حماية: صلاحيات خاصة للمدير فقط.
🛠️ التقنيات المستخدمة (Tech Stack)
الواجهة الأمامية (Frontend)
HTML5 & Vanilla JavaScript: لبناء هيكل وتفاعل سريع وخفيف.
Tailwind CSS: لتصميم عصري ومتجاوب (Responsive).
SweetAlert2: لنوافذ التنبيه والتأكيد التفاعلية.
FontAwesome: للأيقونات.
Axios: للتعامل مع طلبات الـ API.
الواجهة الخلفية (Backend)
Node.js: بيئة التشغيل.
Express.js: إطار عمل الخادم.
JWT (JSON Web Tokens): لتأمين المصادقة وحماية المسارات.
Bcrypt: لتشفير كلمات المرور.
قاعدة البيانات (Database)
MongoDB Atlas: قاعدة بيانات سحابية (NoSQL) لتخزين البيانات بمرونة وأمان.
📂 هيكلية المشروع
LibyaParts/
├── models/             # نماذج قاعدة البيانات (Schema)
│   ├── User.js         # نموذج المستخدم
│   ├── Part.js         # نموذج القطعة
│   └── Request.js      # نموذج الطلب
│
├── public/             # ملفات الواجهة الأمامية
│   ├── index.html      # الصفحة الرئيسية
│   ├── app.js          # منطق الصفحة الرئيسية
│   ├── auth.html       # صفحة التسجيل والدخول
│   ├── auth.js         # منطق المصادقة
│   ├── dashboard.html  # لوحة التاجر
│   ├── admin.js        # منطق لوحة التاجر
│   ├── superadmin.html # لوحة المدير
│   ├── superadmin.js   # منطق لوحة المدير
│   └── pending.html    # صفحة الانتظار
│
├── .env                # متغيرات البيئة (سرية)
├── server.js           # الخادم الرئيسي ونقاط الـ API
├── cleanup.js          # أداة تنظيف قاعدة البيانات
└── package.json        # المكتبات والتبعيات
🚀 دليل التثبيت والتشغيل (Installation)
1. المتطلبات المسبقة
تثبيت Node.js على جهازك.
حساب في MongoDB Atlas (للحصول على رابط الاتصال).
2. تثبيت المشروع
# تثبيت المكتبات المطلوبة
npm install
3. إعداد البيئة
أنشئ ملف 
.env
 في المجلد الرئيسي وأضف البيانات التالية:

MONGODB_URI=رابط_قاعدة_البيانات_الخاص_بالمشروع
JWT_SECRET=رمز_سري_طويل_وقوي
ADMIN_EMAIL=بريد_المدير@gmail.com
PORT=3000
4. تشغيل الخادم
# وضع التطوير (مع إعادة التشغيل التلقائي)
npm run dev
# أو التشغيل العادي
node server.js
افتح المتصفح على: http://localhost:3000

⚙️ أدوات مساعدة (Utilities)
أداة تنظيف قاعدة البيانات
تم توفير أداة لحذف البيانات (مفيد أثناء التطوير والاختبار):

node cleanup.js
ملاحظة: هذه الأداة تحذف البيانات ولكنها تستثني حساب المدير المحدد في ملف .env.

🛡️ الصلاحيات والأمان
المدير: يتم تحديده عبر البريد الإلكتروني في ملف 
.env
. البريد الافتراضي: abdo@gmail.com
التجار: يجب أن يوافق المدير عليهم من لوحة التحكم (
superadmin.html
) قبل أن يتمكنوا من الدخول للوحة التاجر.
الزبائن: يمكنهم التسجيل والدخول فوراً، لكن لا يملكون صلاحيات الوصول للوحات التحكم.
🌐 النشر (Deployment)
المشروع جاهز للنشر على منصات مثل Render أو Vercel. يرجى مراجعة ملف 
deployment.md
 للحصول على خطوات النشر التفصيلية.

تم التطوير بحب وإتقان ❤️ لخدمة السوق الليبي
